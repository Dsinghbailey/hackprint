function setup() {
/* here is the code to create the canvas and attach it to the clothing*/
var cnv=createCanvas(containerWidth, containerHeight, SVG);
var cont =document.getElementById('canvasContainer');
cont.appendChild(cnv.elt.wrapper);

}

function draw() {
  
}
function keyPressed(){
  
}